#!/bin/sh
# Pre-push: run the same test invocation CI runs (.github/workflows/go.yml),
# so failures surface locally instead of on the PR. Bypass in an emergency
# with `git push --no-verify`.
set -e
cd "$(git rev-parse --show-toplevel)"
make test
