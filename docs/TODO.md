# TODO

- [ ] **[7]** Split `src/mcp.rs` (~2000 lines) into sub-modules: server
  struct, tool implementations, batch/structural helpers.
- [ ] **[10]** Set `HOMEBREW_TAP_TOKEN` secret on the canopy GitHub repo
  for the release workflow's Homebrew formula update step.
